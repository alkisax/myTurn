// Queue δεδομένα του setup και η μικρή μορφή για selectors.
export interface QueueSummary {
  id: number;
  companyId: number;
  locationId: number;
  name: string;
}

export interface QueueOption {
  id: number;
  name: string;
}
