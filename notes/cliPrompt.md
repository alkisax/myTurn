Replace the current web favicon with a new SVG favicon more appropriate for MyTurn.

Requirements:
- Keep using `/favicon.svg`.
- Do not change application behavior.
- Create a simple, recognizable favicon related to digital queue/ticketing.
- Prefer a minimal ticket / queue-number concept.
- It must remain readable at very small sizes such as 16x16 and 32x32.
- Use the existing MyTurn visual identity where appropriate.
- Avoid detailed illustrations, gradients, text, or tiny decorative elements.
- Update only the favicon asset and the HTML reference if necessary.
- Do not add new dependencies.

number screen always visible